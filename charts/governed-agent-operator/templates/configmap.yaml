apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "governed-agent-operator.fullname" . }}-config
  labels:
    {{- include "governed-agent-operator.labels" . | nindent 4 }}
data:
  apiUrl: {{ required "api.url is required" .Values.api.url | quote }}

