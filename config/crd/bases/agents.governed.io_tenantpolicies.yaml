---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: tenantpolicies.agents.governed.io
spec:
  group: agents.governed.io
  names:
    kind: TenantPolicy
    listKind: TenantPolicyList
    plural: tenantpolicies
    shortNames:
    - eqpol
    singular: tenantpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TenantPolicy is the Schema for the tenantpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TenantPolicySpec defines the desired state of a TenantPolicy.
            properties:
              allowedModels:
                description: AllowedModels is the model whitelist for the tenant.
                items:
                  type: string
                minItems: 1
                type: array
              allowedTools:
                description: AllowedTools is the tool whitelist for the tenant.
                items:
                  type: string
                type: array
              approvalLevel:
                description: 'ApprovalLevel: "none" | "auto" | "human".'
                enum:
                - none
                - auto
                - human
                type: string
              budget:
                description: Budget declares tenant budgets.
                properties:
                  maxConcurrentRuns:
                    description: MaxConcurrentRuns caps concurrent runs.
                    type: integer
                  maxRunsPerDay:
                    description: MaxRunsPerDay caps total runs per day.
                    type: integer
                  maxTokensPerDay:
                    description: MaxTokensPerDay caps total tokens per day.
                    type: integer
                type: object
              network:
                description: Network declares egress restrictions.
                properties:
                  defaultDenyEgress:
                    description: DefaultDenyEgress when true denies all unlisted egress.
                    type: boolean
                  egressAllowed:
                    description: EgressAllowed lists allowed egress targets (gateway
                      names).
                    items:
                      type: string
                    type: array
                type: object
              policyVersion:
                description: PolicyVersion for auditing.
                minLength: 1
                type: string
              rules:
                description: Rules is a coarse-grained RBAC allow list.
                items:
                  description: PolicyRule declares a coarse-grained allow rule.
                  properties:
                    resources:
                      description: Resources lists resource kinds this rule applies
                        to.
                      items:
                        type: string
                      type: array
                    verbs:
                      description: Verbs lists allowed actions.
                      items:
                        type: string
                      type: array
                  type: object
                type: array
            required:
            - policyVersion
            type: object
          status:
            description: TenantPolicyStatus defines the observed state of a TenantPolicy.
            properties:
              active:
                description: Active indicates the policy is enforced.
                type: boolean
              conditions:
                description: Conditions convey detailed status.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration tracks the last processed generation.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
